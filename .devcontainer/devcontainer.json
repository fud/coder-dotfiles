{
  "name": "coder-dotfiles",
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu",
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y git-lfs && git lfs install --local && bash .devcontainer/scripts/install-nix.sh",
  "postStartCommand": "bash .devcontainer/scripts/install-nix.sh && bash .devcontainer/scripts/start-nix-daemon.sh"
}
